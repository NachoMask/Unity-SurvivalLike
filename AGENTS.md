# 프로젝트 작업 지침

- `Docs/GameDesign.md`의 내용을 토대로 기능 및 코드 제안과 리뷰를 수행한다.
- 코드와 기획서가 충돌하면 임의로 한쪽을 변경하지 말고 충돌 내용을 알린다.
- 사용자가 기획 변경을 명시적으로 확정하고 관련 문서 반영을 요청한 경우 해당 문서도 함께 갱신한다.

## 파일 변경 원칙

- 사용자가 `구현`, `반영` 등 실제 파일 변경을 명시적으로 요청한 경우에만 파일을 변경한다.
- 의견, 검토, 확인, 설명 또는 구현 방법을 묻는 요청에는 파일을 변경하지 않고 분석과 제안만 제공한다.
- 파일 변경이 필요해 보여도 명시적인 변경 요청이 없으면 필요성과 변경 대상을 먼저 알리고 요청 전까지 수정하지 않는다.

## 아키텍처 및 구현 원칙

- 현재 확정된 요구사항을 만족하는 가장 단순한 구조를 우선한다.
- 아직 요구되지 않은 확장성이나 재사용성을 위해 인터페이스, 추상 계층, 래퍼 또는 관리 클래스를 미리 추가하지 않는다.
- 항상 함께 사용되고 같은 이유로 변경되는 상태와 동작은 우선 하나의 클래스로 유지한다.
- 책임과 생명주기가 명확히 다르거나, 실제로 여러 시스템에서 재사용하거나, 분리된 테스트가 현재 필요한 경우에만 클래스를 나눈다.
- 새 클래스를 만들기 전에 기존 클래스를 확장하는 방법으로 충분한지 먼저 검토한다.
- 하나의 기능을 구현하기 위해 새 런타임 스크립트를 3개 이상 추가하거나 새로운 추상 계층을 도입하려면, 먼저 필요성과 더 단순한 대안을 사용자에게 설명한다.
- 미래에 필요할 가능성만으로 구조를 복잡하게 만들지 않는다.

## 검증 및 오류 처리 원칙

- 오류 검증과 처리 방식에 관해서는 이 절의 원칙을 다른 아키텍처 및 구현 원칙보다 우선한다.
- 외부 입력, 직렬화 데이터, 설정 파일 및 네트워크 응답은 시스템 진입점이나 초기화 단계에서 한 번 검증한다.
- 런타임에 변경할 수 있는 데이터는 값을 적용하는 경계에서 검증하고, 매 프레임 실행되는 핵심 로직에서 같은 검사를 반복하지 않는다.
- 초기화 단계에서 검증되었고 외부에서 변경할 수 없는 내부 데이터는 이후의 private 핵심 로직에서 유효성을 신뢰한다.
- public API는 외부 호출 경계로 간주하여 필요한 입력 검증을 수행한다.
- 설정 오류는 초기화 단계에서 검증하여 실행을 중단하고, 프로그래머 오류나 불변식 위반은 조용히 무시하지 말고 즉시 드러나게 처리한다.
- 정상적인 기능 규칙에 해당하는 조건은 검증 코드로 숨기지 않고 핵심 로직에 명시적으로 유지한다.
- `Try` 패턴은 호출자가 실패 시 재시도, 대체 값 사용 또는 건너뛰기와 같은 정상적인 복구를 할 수 있을 때만 사용한다.
- 정상적으로 발생해서는 안 되는 상태를 `false`나 조기 반환으로 숨기거나 매 프레임 재시도하지 않는다.
- 검증 함수는 가능하면 로그 출력이나 상태 변경 없이 검증 결과와 구체적인 오류 내용을 반환하고, 로그 출력과 실행 중단은 호출 경계에서 한 번 처리한다.
- 오류 메시지에는 실패한 대상을 식별할 수 있는 이름이나 인덱스를 포함한다.
- 검증 책임은 우선 해당 데이터를 소유하거나 사용하는 기존 클래스에 둔다.
- 여러 시스템에서 실제로 같은 검증을 재사용하기 전에는 별도의 Validator 클래스, 인터페이스 또는 추상 계층을 추가하지 않는다.
- 편집기 속성과 입력 제약은 입력 편의를 위한 보조 수단으로 취급하며, 런타임에 우회될 수 있는 값은 필요한 경계 검증을 유지한다.

## 리뷰 원칙

- 사용자가 리뷰를 요청하면 변수명과 함수명의 명확성 및 일관성, 클래스와 컴포넌트의 책임 및 의존 관계, 구조상 수정이 필요한 부분을 중심으로 검토한다.
- 추가 구현을 추천할 때는 현재 변경사항의 완성도와 커밋 범위를 함께 고려한다.
- 추가 구현 권장사항은 현재 커밋에 함께 포함할 항목과 별도의 후속 커밋으로 분리할 항목을 구분하여 제안한다.

## 커밋 메시지 작성 규칙

- 커밋 메시지 작성을 요청받으면 현재 대화만 참고하지 말고 Git의 스테이징, 미스테이징, 미추적 변경사항을 직접 확인한다.
- 커밋 제목은 Conventional Commits 형식의 `type: 요약`으로 작성하고 작업 영역(scope)은 사용하지 않는다.
- `type`은 변경 성격에 따라 `feat`, `fix`, `refactor`, `docs`, `test`, `build`, `chore` 중에서 선택한다.
- 제목과 설명은 한국어로 간결하고 직관적으로 작성한다.
- 커밋 설명은 실제 구현 내용만 최대 4개의 항목으로 요약한다.
- 커밋 설명은 파일명 나열보다 구현 결과와 동작 변화를 중심으로 작성하며, 변경 의도를 이해하거나 대상을 구분하는 데 파일명이 필요한 경우에만 명시한다.
- 현재 커밋과 무관한 변경이나 Unity 자동 생성·마이그레이션 파일이 있으면 별도로 구분해 알린다.
- 필요한 Unity 에셋의 `.meta` 파일과 패키지 및 프로젝트 설정이 누락되지 않았는지 확인한다.
- 사용자가 명시적으로 요청하지 않으면 파일을 스테이징하거나 커밋하지 않는다.

출력은 다음 순서를 따른다.

1. 커밋 메시지
2. 최대 3개 항목의 커밋 설명
3. 커밋 제외 권장 변경사항이 있을 경우 해당 목록
4. 수행한 빌드 또는 테스트 결과
